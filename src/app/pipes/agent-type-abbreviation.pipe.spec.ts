import { AgentTypeAbbreviationPipe } from './agent-type-abbreviation.pipe';

describe('AgentTypeAbbreviationPipe', () => {
  it('create an instance', () => {
    const pipe = new AgentTypeAbbreviationPipe();
    expect(pipe).toBeTruthy();
  });
});
