import { AbsenceTypeAbbreviationPipe } from './absence-type-abbreviation.pipe';

describe('AbsenceTypeAbbreviationPipe', () => {
  it('create an instance', () => {
    const pipe = new AbsenceTypeAbbreviationPipe();
    expect(pipe).toBeTruthy();
  });
});
